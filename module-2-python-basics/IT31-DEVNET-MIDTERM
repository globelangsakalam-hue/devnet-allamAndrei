"""
Midterm Practical Exam — Network Device Inventory Tool
Student: Allam, Andrei
""""

devices = []

def display_menu():
    
    print(" === Network Device Inventoy ===")
    print(
          "1. Add a device", 
          "2. View all devices", 
          "3. Count active vs inactive devices", 
          "4. Find a device by name", 
          "5. Exit", 
          "Choose an option:")
display_menu()


def add_device(device_list):
    dev = input("Enter your device name: ")
    print(f"now set your ip & Status:")  
    dev = input("IP Address: ")
    print(f"now, is it active or inactive?")
    dev = input("")

add_device(input)



def view_devices(device_list):

for devices in list
    print(f"Here are your device{devices}.")
        if list == devices:
            print(f"device found!{device}    
    break

else: 
print(f"device not found{device]")




    

